apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: todo-hudai-xyz
  namespace: istio-system
spec:
  secretName: todo-hudai-xyz-tls
  issuerRef:
    name: portpolio-clusterissuer
    kind: ClusterIssuer
  commonName: todo.hudai.xyz
  dnsNames:
    - todo.hudai.xyz